# Sessions (obligatoire sur Vercel)
MONGO_URI=mongodb+srv://user:password@cluster.mongodb.net/ipsa?retryWrites=true&w=majority

# Secret pour signer les cookies de session (générez une chaîne aléatoire longue)
SESSION_SECRET=changez-moi-en-production

# Optionnel — Vercel définit NODE_ENV=production automatiquement
# NODE_ENV=production
